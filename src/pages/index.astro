<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inflación - Calculadora del valor del dinero</title>
</head>
<body>
<style>
  body {
    margin: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #f8faff, #dbe5ff);
  }

  main {
    max-width: 1000px;
    margin: 0 auto;
    padding: 2rem;
  }

  section {
    background-color: #ffffff;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    margin-bottom: 2rem;
    transition: transform 0.3s ease;
  }

  section:hover {
    transform: translateY(-4px);
  }

  h1 {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: #2c3e50;
  }

  h2 {
    font-size: 2rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: #2c3e50;
  }

  h3 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
    text-align: center;
    color: #2c3e50;
  }

  p {
    color: #555;
    line-height: 1.6;
  }

  footer {
    text-align: center;
    margin-top: 2rem;
    color: #777;
    font-size: 0.9rem;
    font-style: italic;
    padding-top: 1rem;
    border-radius: 0 0 12px 12px;
  }

  .welcome-content {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 2rem;
  }

  .welcome-text {
    flex: 1;
  }

  .welcome-img {
    flex: 1;
    max-width: 350px;
  }

  .welcome-img img {
    width: 100%;
    border-radius: 10px;
  }

  .card {
    max-width: 500px;
    margin: 2rem auto;
    padding: 2rem;
    background-color: #ffffff;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    border-radius: 12px;
  }

  form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  label {
    font-weight: 500;
    color: #34495e;
  }

  input {
    padding: 0.6rem;
    border: 1px solid #ccc;
    border-radius: 8px;
    font-size: 1rem;
    transition: border 0.3s ease;
  }

  input:focus {
    outline: none;
    border-color: #3498db;
  }

  #result {
    margin-top: 1.5rem;
    font-weight: bold;
    text-align: center;
    color: #27ae60;
    font-size: 1.2rem;
  }

  @media (max-width: 768px) {
    .welcome-content {
      flex-direction: column;
      text-align: center;
    }
  }
</style>

<main>
  <!-- SECCIÓN DE BIENVENIDA -->
  <section>
    <div class="welcome-content">
      <div class="welcome-text">
        <h1>Inflación y valor del dinero</h1>
        <p>La <strong>inflación</strong> es el aumento general de los precios con el tiempo. Esto hace que el dinero pierda valor, ya que con la misma cantidad puedes comprar menos cosas.</p>
        <p>
          Por ejemplo, si en 2010 tenías $1000, ese dinero no vale lo mismo en 2025. La inflación disminuye su poder adquisitivo.
        </p>
      </div>
      <div class="welcome-img">
        <img src="https://cdn-icons-png.flaticon.com/512/1170/1170576.png" alt="Inflación" />
      </div>
    </div>
  </section>

  <!-- SECCIÓN DE CALCULADORA -->
  <section style="display: flex; justify-content: center; flex-direction: column; align-items: center;">
    <h2>Calculadora de pérdida del valor del dinero</h2>
    <div class="card">
      <h3>Simula cuánto ha perdido tu dinero con la inflación</h3>

      <form id="calc-form">
        <label>
          💵 Cantidad inicial (MXN):
          <input type="number" id="monto" placeholder="1000" required />
        </label>

        <label>
          📅 Año inicial:
          <input type="number" id="anioInicio" placeholder="2010" required />
        </label>

        <label>
          📅 Año final:
          <input type="number" id="anioFinal" placeholder="2025" required />
        </label>

        <label>
          📈 Inflación promedio anual (%):
          <input type="number" id="inflacion" placeholder="5" step="0.1" required />
        </label>
      </form>

      <p id="result">El valor actual aparecerá aquí.</p>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    Hecho por Austin Jaimes Cruz
  </footer>
</main>

<script type="module">
  const form = document.querySelector('#calc-form');
  const result = document.querySelector('#result');

  form?.addEventListener('input', () => {
    const monto = parseFloat(document.querySelector('#monto')?.value || '0');
    const anioInicio = parseInt(document.querySelector('#anioInicio')?.value || '0');
    const anioFinal = parseInt(document.querySelector('#anioFinal')?.value || '0');
    const inflacion = parseFloat(document.querySelector('#inflacion')?.value || '0') / 100;

    if (!monto || !anioInicio || !anioFinal || !inflacion) {
      result.textContent = 'Completa todos los campos.';
      return;
    }

    const años = anioFinal - anioInicio;
    if (años < 0) {
      result.textContent = '⚠️ El año final debe ser mayor al inicial.';
      return;
    }

    // Fórmula: valorActual = monto * (1 - inflacion)^años
    const valorActual = monto * Math.pow(1 - inflacion, años);
    const perdida = monto - valorActual;

    result.innerHTML = `
      En ${años} años, tu dinero perdió aproximadamente 
      <span style="color:#e74c3c;">$${perdida.toFixed(2)}</span> de su valor.<br>
      Hoy equivale a <span style="color:#27ae60;">$${valorActual.toFixed(2)}</span>.
    `;
  });
</script>

</body>
</html>
