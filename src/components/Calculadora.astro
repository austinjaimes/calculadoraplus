---
---

// No necesitas importar useState ni nada más

<style>
  body {
    margin: 0;
    background-color: #f3f6f9;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  }

  main {
    max-width: 900px;
    margin: 0 auto;
    padding: 2rem;
  }

  section {
    background-color: #ffffff;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    margin-bottom: 2rem;
  }

  h1, h2, h3 {
    color: #2c3e50;
  }

  p {
    color: #555;
    line-height: 1.6;
  }

  .card {
    max-width: 500px;
    margin: 2rem auto;
    padding: 2rem;
    background-color: #ffffff;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    border-radius: 12px;
  }

  form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  label {
    font-weight: 500;
    color: #34495e;
  }

  input, select {
    padding: 0.6rem;
    border: 1px solid #ccc;
    border-radius: 8px;
    font-size: 1rem;
    transition: border 0.3s ease;
  }

  input:focus {
    outline: none;
    border-color: #3498db;
  }

  #result {
    margin-top: 1.5rem;
    font-weight: bold;
    text-align: center;
    color: #27ae60;
    font-size: 1.2rem;
  }

  hr {
    margin: 3rem 0;
    border: none;
    border-top: 1px solid #ddd;
  }
</style>

<main>
  <section>
    <h1>Bienvenido</h1>
    <p>El ahorro y la inversión son claves para lograr estabilidad financiera.</p>
    <p>
      El <strong>ahorro</strong> es separar parte de tus ingresos para el futuro, mientras que la <strong>inversión</strong> busca hacer crecer ese dinero a través del tiempo.
    </p>
  </section>

  <section>
    <h2>Calculadora de ahorro o inversión</h2>

    <div class="card">
      <h3>Simula tu ahorro o inversión</h3>

      <form id="calc-form">
        <label>
          Monto inicial:
          <input type="number" id="monto" placeholder="1000" />
        </label>

        <label>
          Tasa de interés anual (%):
          <input type="number" id="tasa" placeholder="5" />
        </label>

        <label>
          Años:
          <input type="number" id="tiempo" placeholder="3" />
        </label>

        <label>
          Tipo de cálculo:
          <select id="modo">
            <option value="inversion">Inversión (con intereses)</option>
            <option value="ahorro">Solo ahorro (sin intereses)</option>
          </select>
        </label>
      </form>

      <p id="result">Monto final estimado: $0.00</p>
    </div>
  </section>
</main>

<script type="module" client:load>
  const form = document.querySelector('#calc-form');
  const result = document.querySelector('#result');

  form?.addEventListener('input', () => {
    const monto = parseFloat(document.querySelector('#monto')?.value || '0');
    const tasa = parseFloat(document.querySelector('#tasa')?.value || '0');
    const tiempo = parseFloat(document.querySelector('#tiempo')?.value || '0');
    const modo = document.querySelector('#modo')?.value;

    let resultado = 0;

    if (modo === 'inversion') {
      resultado = monto * Math.pow(1 + tasa / 100, tiempo);
    } else {
      resultado = monto * tiempo; // Ahorro sin interés
    }

    result.textContent = `Monto final estimado: $${resultado.toFixed(2)}`;
  });
</script>
